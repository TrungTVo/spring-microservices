server:
  port: 9097

spring:
  application:
    name: school-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instanceId: ${spring.application.name}:${random.value}
        enabled: true
        register: true    # default to be true, if want to deregister -> false
